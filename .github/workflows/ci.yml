name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.9", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install
        run: pip install -e ".[dev]"
      - name: Lint
        run: ruff check .
      - name: Tests
        run: pytest -q
      - name: Demo run (no API key needed)
        run: |
          python -m revops run --out /tmp/demo
          test -s /tmp/demo/weekly_digest.md

  docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build image
        run: docker build -t revops .
      - name: Smoke run (deterministic mode, bundled samples)
        run: |
          mkdir -p /tmp/reports
          docker run --rm -v /tmp/reports:/reports revops
          test -s /tmp/reports/weekly_digest.md
